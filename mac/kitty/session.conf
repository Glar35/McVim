# Optional startup session referenced by kitty.conf (`startup_session`).
# Edit the cd path to wherever you want Kitty to open, or delete the
# `startup_session` line in kitty.conf to start in your home directory.
cd ~
launch
